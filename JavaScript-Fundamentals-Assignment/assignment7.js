let shoppingListApp = ["Mobiles", "Keyboard", "Mouse", "Accessories", "Adaptor", "Earphones"]

console.log("All the items in the shopping list array are :");
for (let i = 0; i < shoppingListApp.length; i++) {
    const items = shoppingListApp[i];
    console.log(items);
}